import { initializePlayerController } from './application/playerController.js';

/**
 * Initializes all application-layer logic for the Player Simulation feature.
 */
export function initializePlayerSimulationFeature() {
    initializePlayerController();
}