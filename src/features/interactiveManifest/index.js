import { initializeInteractiveManifestController } from './application/interactiveManifestController.js';

/**
 * Initializes all application-layer logic for the Interactive Manifest feature.
 */
export function initializeInteractiveManifestFeature() {
    initializeInteractiveManifestController();
}
