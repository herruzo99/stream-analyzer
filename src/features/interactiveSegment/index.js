import { initializeInteractiveSegmentController } from './application/interactiveSegmentController.js';

/**
 * Initializes all application-layer logic for the Interactive Segment feature.
 */
export function initializeInteractiveSegmentFeature() {
    initializeInteractiveSegmentController();
}
